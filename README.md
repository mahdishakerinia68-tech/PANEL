# STRIX VIP PANEL V1.2

ورود مدیر اولیه:
- username: `mahdi`
- password: `mahdi6812`

این ادمین فقط در صورتی به‌صورت خودکار ساخته می‌شود که جدول admins خالی باشد. اگر Backend قبلاً اجرا شده و دیتابیس `strix.db` ادمین دیگری دارد، آن دیتابیس را نگه دار و از بخش «حساب کاربری» مشخصات را تغییر بده؛ برای شروع کاملاً تازه، `strix.db` را فقط در سرور Backend حذف کن تا دوباره ادمین اولیه ساخته شود.

Backend باید روی سرور اجرا شود؛ GitHub Pages به‌تنهایی API `/api` را اجرا نمی‌کند.
