# STRIX VIP PANEL v1.2

ادمین اولیه قابل تغییر است:
- Username: `mahdi`
- Password: `mahdi6812`

برای تغییر پیش‌فرض، متغیرهای `ADMIN_USER` و `ADMIN_PASSWORD` را در `.env` تنظیم کنید. همچنین بعد از ورود، بخش مدیران امکان تغییر ادمین را دارد.
