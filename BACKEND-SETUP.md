# STRIX VIP PANEL v1.3
در این نسخه مدیریت ادمین اضافه شده است:
- ساخت ادمین جدید
- تغییر نام کاربری ادمین
- تغییر رمز ادمین
- حذف ادمین
- جلوگیری از حذف آخرین ادمین
- نگهداری رمزها به صورت hash با bcrypt
- ورود مدیر از دیتابیس

برای استقرار واقعی، `.env` را تنظیم کن و Backend را روی یک سرور اجرا کن. مقدارهای ADMIN_USER و ADMIN_PASSWORD فقط برای bootstrap/fallback هستند؛ بعد از ساخت ادمین‌های دیتابیسی می‌توانی آن‌ها را تغییر/حذف کنی.
